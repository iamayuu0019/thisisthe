<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cyberpunk Login</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
            overflow: hidden;
        }
        .login-box {
            position: relative;
            width: 400px;
            padding: 50px;
            background: rgba(0, 0, 0, 0.8);
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.8);
            text-align: center;
            animation: neonGlow 1.5s infinite alternate;
        }
        @keyframes neonGlow {
            from { box-shadow: 0 0 20px rgba(0, 255, 255, 0.8); }
            to { box-shadow: 0 0 40px rgba(0, 255, 255, 1), 0 0 80px rgba(0, 255, 255, 0.5); }
        }
        .login-box h2 {
            color: cyan;
            text-shadow: 0 0 20px cyan;
            margin-bottom: 20px;
        }
        .input-container {
            position: relative;
            margin: 20px 0;
        }
        .input-container input {
            width: 100%;
            padding: 10px;
            background: transparent;
            border: 2px solid cyan;
            border-radius: 5px;
            font-size: 18px;
            color: cyan;
            transition: 0.3s;
            outline: none;
        }
        .input-container input:focus {
            border-color: white;
            box-shadow: 0 0 15px cyan;
        }
        .login-btn {
            width: 100%;
            padding: 12px;
            background: cyan;
            border: none;
            color: black;
            font-size: 18px;
            cursor: pointer;
            border-radius: 5px;
            transition: 0.3s;
            box-shadow: 0 0 20px cyan;
        }
        .login-btn:hover {
            background: white;
            box-shadow: 0 0 40px cyan, 0 0 80px white;
        }
        .glitch {
            font-size: 20px;
            color: cyan;
            text-shadow: 2px 2px 5px rgba(0, 255, 255, 0.6);
            animation: glitchEffect 1.5s infinite;
        }
        @keyframes glitchEffect {
            0% { transform: skewX(0deg); }
            20% { transform: skewX(-10deg); }
            40% { transform: skewX(10deg); }
            60% { transform: skewX(-5deg); }
            80% { transform: skewX(5deg); }
            100% { transform: skewX(0deg); }
        }
    </style>
</head>
<body>
    <div class="login-box">
        <h2>Cyberpunk Login</h2>
        <div class="input-container">
            <input type="text" placeholder="Username" required>
        </div>
        <div class="input-container">
            <input type="password" placeholder="Password" required>
        </div>
        <button class="login-btn">Enter</button>
        <p class="glitch">Access Restricted</p>
    </div>
</body>
</html>
